---
"@dustinbyrne/kb": patch
---

Ensure kb engine agent sessions load models from the user pi agent directory (`~/.pi/agent/models.json`) so configured models remain available when inheriting user settings/plugins.
